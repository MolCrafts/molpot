[project]
name = "molpot"
version = "0.0.1"
dependencies = [
    "numpy",
    "torch_scatter",
    "torchdata",
    "portalocker",
    "typer"
]

[project.scripts]
molpot = "molpot.cli:app"

[build-system]
requires = [
    "setuptools",
]
build-backend = "setuptools.build_meta"
