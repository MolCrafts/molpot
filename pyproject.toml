[project]
name = "molpot"
version = "0.0.1"
dependencies = [
    "numpy",
    "molpy@git+https://github.com/MolCrafts/molpy.git@dev"
]
[project.scripts]

[build-system]
requires = [
    "setuptools",
]
build-backend = "setuptools.build_meta"